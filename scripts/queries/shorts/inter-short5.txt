// Given a Message with ID $messageId, retrieve its author

/*
:param messageId: 206158431836
*/
MATCH (m:Message {id:  $messageId })-[:hasCreator]->(p:Person)
RETURN
    p.id AS personId,
    p.firstName AS firstName,
    p.lastName AS lastName